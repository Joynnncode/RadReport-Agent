[pytest]
testpaths = tests
markers =
    slow: loads a real model, takes seconds (deselect with -m "not slow")
    network: hits a live external API (deselect with -m "not network")
filterwarnings =
    ignore::DeprecationWarning
